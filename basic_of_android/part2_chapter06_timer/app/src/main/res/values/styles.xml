<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:statusBarColor">@color/pomodoro</item>
        <item name="android:windowBackground">@color/pomodoro</item>
    </style>

</resources>
